# AppCache-Lock Python Dependencies
# Install all dependencies with: pip install -r requirements.txt

# System and process monitoring
psutil>=5.8.0

# System requirements:
# - Python 3.6+
# - vmtouch (install with: sudo apt install vmtouch)
# - systemd (for service installation)
